import { makeSeriesCard } from "../series-card";

export const StackedAreaChart = makeSeriesCard(
  "stackedArea",
  "visualize_cumulative_user_over_time",
  "StackedAreaChart"
);

export default StackedAreaChart;
