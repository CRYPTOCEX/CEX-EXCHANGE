// The same key as the desk console it splits off from. This page is the same
// data in a different shape — an operator who may work the queue may read the
// archive, and a separate key would only mean one of the two silently 404s for
// half the desk after an upgrade.
export const permission = "access.support.ticket";
